<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Server Action: Bulk Sync Groups -->
        <record id="action_bulk_sync_groups" model="ir.actions.server">
            <field name="name">Bulk Sync Groups</field>
            <field name="model_id" ref="model_whatsapp_group"/>
            <field name="binding_model_id" ref="model_whatsapp_group"/>
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="code">action = records.action_bulk_sync_groups()</field>
        </record>

        <!-- Server Action: Bulk Send Message -->
        <record id="action_bulk_send_message" model="ir.actions.server">
            <field name="name">Send Message to Groups</field>
            <field name="model_id" ref="model_whatsapp_group"/>
            <field name="binding_model_id" ref="model_whatsapp_group"/>
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="code">action = records.action_bulk_send_message()</field>
        </record>

        <!-- Server Action: Bulk Generate Invite Codes -->
        <record id="action_bulk_generate_invite_codes" model="ir.actions.server">
            <field name="name">Generate Invite Codes</field>
            <field name="model_id" ref="model_whatsapp_group"/>
            <field name="binding_model_id" ref="model_whatsapp_group"/>
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="code">action = records.action_bulk_generate_invite_codes()</field>
        </record>

        <!-- Server Action: Bulk Update Members -->
        <record id="action_bulk_update_members" model="ir.actions.server">
            <field name="name">Update Group Members</field>
            <field name="model_id" ref="model_whatsapp_group"/>
            <field name="binding_model_id" ref="model_whatsapp_group"/>
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="code">action = records.action_bulk_update_members()</field>
        </record>
    </data>
</odoo>